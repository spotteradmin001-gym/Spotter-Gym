import { describe, expect, it } from "vitest";

// Phase 0 placeholder so `npm test` is a real gate from the first commit.
// Replaced by feature suites as phases land.
describe("smoke", () => {
  it("runs the test toolchain", () => {
    expect(1 + 1).toBe(2);
  });
});
